# Deployment

Not in scope yet. Phase 1–4 target local development only, per the project
brief. Phase 5 will document a deployment path (e.g. Docker Compose for
backend + Postgres, static hosting for the built frontend) once the full
feature set is stable.
